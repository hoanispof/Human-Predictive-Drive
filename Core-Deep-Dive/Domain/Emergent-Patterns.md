# Emergent Patterns — Khi Domain Interaction Lặp Lại

> **Bạn gặp 1 người → classify, evaluate, tương tác 1 lần → quên.**
> **Bạn gặp người đó 100 lần → classify, evaluate, tương tác 100 lần →**
> **→ Không còn "1 lần" nữa. Đã thành PATTERN.**
>
> Connection. Kẻ thù. Người nuôi dưỡng. Người cho đi.
> Phụ thuộc. Yêu lẫn ghét. Nhóm bạn. Phản bội.
>
> Những thứ này KHÔNG được thiết kế sẵn.
> Chúng EMERGE — từ domain interaction lặp đi lặp lại,
> valence tích lũy, schema compiled qua thời gian.
>
> File này: TOÀN BỘ patterns đó là gì, emerge thế nào,
> và tại sao chúng mạnh đến mức chi phối cuộc sống.

---

> **Trạng thái:** DRAFT v0.5 — synthesis từ 3 sessions phân tích sâu
> **Ngày:** 2026-04-12
> **Mục đích:** Mô tả các EMERGENT PATTERNS từ domain interaction:
> Connection, Enemy, Nurturing, "Cho đi", Dependency, Mixed Valence,
> Group Dynamics, Violation & Recovery.
> Redefine Connection (từ "channel" → emergent pattern + body-base need).
> Redefine Empathy behaviors (từ "mechanism riêng" → natural results of agent processing).
> **Absorb từ:** Connection.md (backup) + Empathy-Mirror.md §6-§8 (backup)
> **Vị trí:** Core-Deep-Dive/Domain/ (sau Valence.md, trước Domain-Mechanisms.md)
> **Flow đọc:**
>   Domain-Interaction.md (overview)
>   ├── Object-Agent.md (classification)
>   ├── Logic-Modeling.md (processing)
>   ├── Valence.md (evaluation)
>   ├── **Emergent-Patterns.md** (file NÀY — patterns emerge từ repeated interaction)
>   └── Domain-Mechanisms.md (mirror, imagine-final, feedback)
> **Tiền đề:**
>   Valence.md (multi-channel assessment → basis cho MỌI patterns)
>   Domain-Interaction.md §6 (Agent Input = body-base need → basis cho Connection)
>   Object-Agent.md (classification → basis cho Agent vs Object patterns)
>   Drive.md (HOW drives combine → action)
>   Imagine-Final.md (preview outcomes → predict bond durability)
> **Liên quan mainstream:**
>   🟢 Social Baseline Theory (Coan & Beckes 2015)
>   🟢 Dunbar's Number (Dunbar 1992, 1998)
>   🟢 Interpersonal synchrony (Feldman 2007)
>   🟢 Social buffering (Kiyokawa 2004)
>   🟢 Social pain = physical pain (Eisenberger 2003)
>   🟢 Baby Schema / Kindchenschema (Lorenz 1943)
>   🟢 Reciprocal altruism (Trivers 1971)
>   🟢 Attachment theory (Bowlby 1969)
> **⚠️ 🟡 Framework synthesis — reframe existing concepts trong Domain Interaction lens**
> **Quy ước:** 🟢 Research support | 🟡 Suy luận từ framework | 🔴 Hypothesis

---

## Mục lục

- §0 — VỊ TRÍ TRONG FRAMEWORK
- §1 — EMERGENT PATTERNS LÀ GÌ
- §2 — CONNECTION PATTERN
- §3 — ENEMY / THREAT PATTERN
- §4 — NURTURING PATTERN
- §5 — "CHO ĐI" PATTERN
- §6 — DEPENDENCY PATTERN
- §7 — MIXED VALENCE (YÊU + GHÉT)
- §8 — GROUP DYNAMICS
- §9 — VIOLATION & RECOVERY
- §10 — HONEST ASSESSMENT
- §11 — CROSS-REFERENCES

---

## §0 — VỊ TRÍ TRONG FRAMEWORK

```
🟡 EMERGENT PATTERNS TRONG KIẾN TRÚC:

  Domain Interaction Flow:
    Body-need → ① Classify → ② Process → ③ Evaluate (Valence) → Drive → Action → Feedback

  Feedback → LẶP LẠI → valence TÍCH LŨY → patterns EMERGE:

  ┌────────────────────────────────────────────────────────────────────┐
  │ BODY-BASE (L0-L3) — needs                                         │
  └──────────────────────────┬─────────────────────────────────────────┘
                             ↓
  ┌────────────────────────────────────────────────────────────────────┐
  │ DOMAIN INTERACTION                                                  │
  │   Classify → Process → Evaluate → Drive → Action → Feedback        │
  │                                                                    │
  │   Loop 1: valence HÌNH THÀNH (entity mới, đánh giá lần đầu)       │
  │   Loop 2-10: valence CẬP NHẬT (reinforce hoặc revise)             │
  │   Loop 10-100: valence COMPILED → schema ổn định                   │
  │   Loop 100+: PATTERN EMERGE ← ⭐ FILE NÀY                         │
  │                                                                    │
  │   ┌──────────────────────────────────────────────────────────────┐ │
  │   │ EMERGENT PATTERNS (compiled từ repeated interaction)          │ │
  │   │                                                              │ │
  │   │   Connection — sustained mutual positive valence             │ │
  │   │   Enemy — sustained negative valence                         │ │
  │   │   Nurturing — giving + vulnerability amplification           │ │
  │   │   "Cho đi" — surplus + mirror reward > keep reward           │ │
  │   │   Dependency — high positive + irreplaceable                 │ │
  │   │   Mixed Valence — positive + negative cùng lúc               │ │
  │   │   Group — multi-agent amplification                          │ │
  │   │   Violation — khi pattern bị phá                             │ │
  │   └──────────────────────────────────────────────────────────────┘ │
  └────────────────────────────────────────────────────────────────────┘


  PHÂN BIỆT:
    Valence.md = HOW body đánh giá 1 entity tại 1 thời điểm
    Emergent-Patterns.md = WHAT xảy ra khi valence TÍCH LŨY qua thời gian
    = 2 file BỔ SUNG:
      Valence: "Bạn B hiện tại = L3 positive nhẹ" (ảnh chụp)
      Patterns: "Bạn B sau 2 năm = CONNECTION compiled" (phim)
```

---

## §1 — EMERGENT PATTERNS LÀ GÌ

```
🟡 ĐỊNH NGHĨA:

  Emergent Pattern = mối quan hệ ổn định giữa body và domain entity,
  được COMPILED từ repeated domain interaction.

  KHÔNG phải module pre-designed:
    → Không có vùng não riêng cho "connection" hay "enemy"
    → Cùng 1 bộ cơ chế (Classify × Process × Valence × Body-base)
    → Nhưng INPUT KHÁC → OUTPUT KHÁC → patterns KHÁC emerge

  KHÔNG phải quyết định có ý thức:
    → Body TỰ TÍCH LŨY valence qua mỗi interaction
    → Patterns COMPILED vào schema vô thức
    → PFC chỉ NHẬN RA pattern đã hình thành ("ừ, nó là bạn tôi")
    → PFC KHÔNG TẠO pattern ("tôi quyết định nó là bạn")


  TẠI SAO GỌI "EMERGENT":

    ① Không ai thiết kế trước:
       → Không có blueprint "connection phải thế này"
       → Interaction LẶP LẠI + valence TÍCH LŨY = pattern TỰ xuất hiện

    ② Emerge từ tương tác, không từ cá nhân:
       → 1 người KHÔNG THỂ có connection pattern (cần 2+)
       → 1 entity KHÔNG TỰ MÌNH thành "enemy" (cần context + body reaction)
       → Pattern nằm GIỮA body và entity, không thuộc bên nào

    ③ Không thể predict chính xác pattern nào sẽ emerge:
       → 2 người gặp nhau → có thể thành bạn, thành kẻ thù, hoặc chẳng thành gì
       → Phụ thuộc: valence tích lũy + context + body-state + cả 2 bên


  CẤU TRÚC MỖI PATTERN:

    ┌────────────────────────────────────────────────────────────────┐
    │ TRIGGER: điều kiện domain interaction nào dẫn tới pattern     │
    │   → Valence profile cụ thể + frequency + duration             │
    │                                                                │
    │ MECHANISM: body xử lý thế nào                                  │
    │   → Classify + Process + Evaluate + Mirror + Schema            │
    │                                                                │
    │ COMPILED STATE: pattern đã hình thành = gì                     │
    │   → Schema compiled + valence profile ổn định                  │
    │                                                                │
    │ INFLUENCE: pattern ảnh hưởng domain interaction tiếp theo       │
    │   → Bias classify, bias evaluate, bias drive                   │
    │                                                                │
    │ BREAK CONDITION: khi nào pattern bị phá / thay đổi             │
    │   → Violation, absence, gradual shift                          │
    └────────────────────────────────────────────────────────────────┘

    → MỌI patterns trong §2-§9 đều follow cấu trúc này
```

---

## §2 — CONNECTION PATTERN

> Section LỚN NHẤT — vì Connection là pattern phức tạp nhất,
> ảnh hưởng sâu nhất, và cần redefine kỹ nhất trong framework.
> Absorb content từ Connection.md (backup).

---

### §2.1 — Agent Input = Body-Base Need

```
⭐ NỀN TẢNG: connection emerge TRÊN body-base need có sẵn.

  Trước khi hiểu CONNECTION PATTERN, phải hiểu:
  → Body CẦN agent input ở mức HARDWARE — không phải "tốt khi có"
  → Agent input thiếu = brain DAMAGE (evidence 🟢 rõ ràng)
  → Connection pattern emerge TRÊN nhu cầu này — không phải ngược lại

  (Evidence chi tiết: Domain-Interaction.md §6)


  TÓM TẮT EVIDENCE:

  🟢 Social Baseline Theory (Coan & Beckes 2015):
    → Não DEFAULT giả định CÓ agent gần
    → Ở 1 mình = deviation = não phải làm thêm việc
    → = Agent input = DEFAULT state, không phải luxury

  🟢 Mere Presence Effect (2024-2025):
    → CHỈ CẦN có agent gần (không cần tương tác)
    → Brain networks TỰ ĐỘNG recalibrate
    → = Tối thiểu Level 0 agent input = ĐÃ có tác dụng

  🟢 Romanian orphans (Bucharest EIP, Nelson et al.):
    → L1 met (food, shelter) + agent input THIẾU → brain DAMAGE
    → = Agent input CẦN cho neural development

  🟢 Solitary confinement (Grassian 2006):
    → L1 met + agent input ≈ 0 → hallucinations trong VÀI NGÀY
    → = Agent input = need ở level THẤP HƠN cả L2 Quality

  🟢 Harlow (1958):
    → Khỉ con chọn touch OVER food
    → = Agent presence = separate need, KHÔNG thay thế bằng L1


  AGENT INPUT SPECTRUM:

  ┌──────────┬──────────────────────────────────────────────────────┐
  │ Level    │ Mô tả                                               │
  ├──────────┼──────────────────────────────────────────────────────┤
  │ Level 0  │ MERE PRESENCE — biết có agent gần                    │
  │          │ Brain auto-recalibrate. Quán cà phê > nhà 1 mình   │
  ├──────────┼──────────────────────────────────────────────────────┤
  │ Level 1  │ PASSIVE OBSERVATION — quan sát agents hoạt động     │
  │          │ Nhặt patterns vô thức, calibrate nhẹ                │
  ├──────────┼──────────────────────────────────────────────────────┤
  │ Level 2  │ LIGHT INTERACTION — chào hỏi, xã giao              │
  │          │ Mirror fire nhẹ, agent PHẢN HỒI → verify mình       │
  ├──────────┼──────────────────────────────────────────────────────┤
  │ Level 3  │ REGULAR INTERACTION — bạn bè, đồng nghiệp           │
  │          │ Chunks exchange, shared novelty, co-regulation nhẹ  │
  ├──────────┼──────────────────────────────────────────────────────┤
  │ Level 4  │ DEEP SUSTAINED — bạn thân, gia đình                 │
  │          │ Melody calibration SÂU, co-regulation MẠNH          │
  │          │ Virtual chunks RELIABLE, error correction            │
  └──────────┴──────────────────────────────────────────────────────┘

  Level 0-1 = BODY-BASE NEED (hardware, brain cần để function)
  Level 2-4 = COMPILED META-DRIVE (ai cụ thể, bao sâu = learned)

  TƯƠNG TỰ HUNGER:
    "Đói" = body-base need (hardware)
    "Thèm phở" = compiled preference (learned)
    "Cần agent input" = body-base need (hardware)
    "Muốn gặp bạn thân" = compiled preference (learned)


  TẠI SAO AGENT INPUT > MỌI INPUT KHÁC (Domain-Interaction.md §6):

    Agent = công cụ map domain HIỆU QUẢ NHẤT:
      ① Independent sensor — body KHÁC verify cùng domain → dual check
      ② Chunks tôi không có → domain data MIỄN PHÍ
      ③ Unpredictable → prediction error tự động → novelty → learning
      ④ Mirror mechanism → auto-calibrate schema (không cần PFC effort)
      ⑤ Phản hồi hành vi tôi → biết tôi map domain đúng/sai

    Gương: 0/5 → trẻ chán sau 1 lúc
    Object: 1-2/5 → cạn novelty nhanh (deterministic)
    Agent: 5/5 → body PREFER agent input vì RICHEST source

  → §2.1 = NỀN TẢNG — connection pattern emerge TRÊN need này
```

---

### §2.2 — Connection = Emergent Pattern

```
🟡 ĐỊNH NGHĨA MỚI (redefine từ Connection.md):

  ┌────────────────────────────────────────────────────────────────┐
  │ CŨ (Connection.md, 2026-03-28):                                │
  │   Connection = emergent property GIỮA melodies                 │
  │   = Harmony trong nhạc — xuất hiện khi 2+ melodies cùng chơi  │
  │   ✓ Đúng hướng (emergent, giữa, không phải channel riêng)     │
  │   ✗ Chưa đặt trong Domain Interaction framework               │
  │   ✗ Chưa giải thích WHY emerge (missing: valence + time)      │
  │                                                                │
  │ MỚI (file này, 2026-04-12):                                    │
  │   Connection = EMERGENT PATTERN từ:                             │
  │     ① Body-base agent input need (Level 0-1, hardware)         │
  │     ② Sustained mutual POSITIVE valence (multi-channel)        │
  │     ③ Repeated interaction over TIME                            │
  │     ④ Compiled vào schema → ổn định                            │
  │                                                                │
  │   = Pattern emerge khi 2 agents TƯƠNG TÁC LẶP LẠI             │
  │     và valence tích lũy POSITIVE cho CẢ HAI bên                │
  │                                                                │
  │   Melody metaphor VẪN ĐÚNG — bổ sung:                          │
  │     Connection = harmony emerge giữa melodies                   │
  │     + NỀN: body-base NEED agent input (L0-1)                   │
  │     + CƠ CHẾ: valence tích lũy qua domain interaction          │
  └────────────────────────────────────────────────────────────────┘


  4 ĐIỀU KIỆN ĐỂ CONNECTION EMERGE:

    ① Agent input need CỦA BODY đang active:
       → Body luôn seek agent input (§2.1)
       → = Đất đã sẵn — chỉ cần hạt giống đúng

    ② 2 agents TƯƠNG TÁC lặp lại:
       → 1 lần gặp = valence hình thành, chưa phải pattern
       → N lần gặp = valence TÍCH LŨY → pattern dần emerge

    ③ Valence tích lũy POSITIVE cho CẢ HAI bên:
       → 1 bên positive + 1 bên negative = KHÔNG emerge connection
       → Cả 2 bên positive nhưng KHÁC channels = VẪN OK
         (A thích B vì novelty, B thích A vì stability = vẫn connection)

    ④ TIME đủ để compile vào schema:
       → Connection cạn (vài lần gặp): dễ mất, chưa compile sâu
       → Connection sâu (nhiều năm): compiled SÂU, khó phá


  CONNECTION = FORCE MULTIPLIER CHO TOÀN BỘ BODY-BASE:

  🟡 Tại sao connection MẠNH đến vậy? Vì nó AMPLIFY mọi layer:

  ┌──────────┬──────────────────────┬────────────────────────────────┐
  │ Layer    │ 1 mình               │ Có connection                   │
  ├──────────┼──────────────────────┼────────────────────────────────┤
  │ L0 Alive │ Tự bảo vệ → YẾU     │ Đàn bảo vệ → sống sót ↑↑↑     │
  │          │ 1 đôi mắt canh      │ 20 đôi mắt canh                │
  ├──────────┼──────────────────────┼────────────────────────────────┤
  │ L1 Surv. │ Tự kiếm ăn → LIMITED│ Phân công → ăn TỐT hơn ↑↑     │
  │          │ Làm mọi thứ → dở    │ Mỗi người 1 thứ → giỏi hết   │
  ├──────────┼──────────────────────┼────────────────────────────────┤
  │ L2 Qual. │ Tự regulate → HARD  │ Co-regulate → EASIER ↑↑        │
  │          │ Stress → tự chịu    │ Stress → ôm → oxytocin         │
  ├──────────┼──────────────────────┼────────────────────────────────┤
  │ L3 Nov.  │ Tự mò → SLOW        │ Learn từ nhau → FAST ↑↑↑      │
  │  Status  │ Không ai đánh giá   │ Social calibrate → rõ vị trí  │
  │  Protect │ Tự bảo vệ "của tôi"│ Group bảo vệ → mạnh hơn       │
  └──────────┴──────────────────────┴────────────────────────────────┘

  → Connection KHÔNG thay thế L0-L3 (vẫn cần ĂN, THỞ, NGỦ)
  → Connection AMPLIFY mọi thứ → "1 + 1 > 2"
  → = Lý do evolution ĐẦU TƯ MẠNH vào social brain:
    🟢 Social Brain Hypothesis (Dunbar 1998): neocortex size ~ group size
    → Não lớn KHÔNG phải để giải toán → để QUẢN LÝ connections


  CONNECTION × IMAGINE-FINAL — layer nào → bond BỀN bao nhiêu:

  🟡 Mỗi connection có Imagine-Final riêng — body preview "ở cùng người này sẽ thế nào?"

    L0/L1 Imagine-Final = NGẮN HẠN:
      → Body-base met → Imagine-Final dormant → connection PHAI
      → Đói → gặp người cho ăn → no → Imagine-Final tắt
      → = Mối quan hệ chỉ dựa trên L0/L1 = fragile

    L2 Imagine-Final = TRUNG HẠN:
      → "Cảm giác tốt khi ở cùng" — body preview → approach
      → Nhưng nếu L2 dissonance tăng → connection GIẢM
      → = Bạn vui vẻ → bạn hay gây stress → giảm dần

    L3 Imagine-Final = BỀN NHẤT:
      → Đạt mốc → Imagine-Final NÂNG CẤP (không tắt)
      → Co-founders đạt milestone → "tiếp, build bigger!"
      → L3 Novelty không có ceiling → Imagine-Final tiến hóa mãi
      → CHỊU ĐƯỢC L2 dissonance: cãi nhau nhưng cùng mission
      → = Dissonance = investment cost cho Imagine-Final chung

    ⚠️ EXTERNAL LOCK — bond bị khóa vào layer thấp:

      Khóa L1: "không làm → không có lương → không ăn"
        → Connection với sếp BỊ KHÓA vào L1
        → Nghỉ việc (mở khóa) → connection BIẾN MẤT
        → = Bond không có gốc, chỉ có khóa

      Khóa L2: "con phải học → mẹ mới chơi với con"
        → Connection bị khóa vào compliance
        → Rủi ro: compile "connection = có điều kiện"

      Khóa L0: "không nghe lời → bỏ rơi"
        → Connection bị khóa vào SURVIVAL
        → = TOXIC nhất: connection gắn threat → trauma compile

    → Bond bị khóa = FRAGILE: tháo khóa → mất ngay
    → Bond BỀN NHẤT = L3 Imagine-Final shared (tự nguyện, intrinsic)


  ⭐ HỆ QUẢ — predict connection depth:

    Biết 2 người share Imagine-Final ở LAYER NÀO → predict:
      Chỉ L1/L2: bond tắt khi need met → "đồng nghiệp cũ"
      L2 + L3: bond trung bình, bền nếu L3 align → "bạn tốt"
      L3 deep: bond bền nhất, chịu xung đột → "tri kỷ, co-founder"

    Biết CÓ KHÓA hay KHÔNG → predict fragility:
      Có khóa (lương, threat, điều kiện) → FRAGILE
      Không khóa (tự nguyện, intrinsic) → DURABLE
```

---

### §2.3 — Virtual Chunks — Mở Rộng Domain Map Ngoài 1 Body

```
🔴 CONNECTION = MỞ RỘNG CAPACITY NGOÀI GIỚI HẠN 1 BODY:

  1 NGƯỜI = chunks CỦA MÌNH:
    → Capacity = brain × experience CỦA TÔI
    → Ceiling CỐ ĐỊNH — 1 não = 1 não
    → Trên đảo hoang: biết gì → dùng đó → hết

  2 NGƯỜI = chunks CỦA MÌNH + ACCESS chunks BẠN:
    → "Tôi không biết mài gỗ lấy lửa → bạn biết → CHÚNG TÔI có lửa"
    → Tôi KHÔNG CẦN compile chunk đó → chỉ cần ACCESS người CÓ
    → = "VIRTUAL CHUNKS" — chunks tôi DÙNG ĐƯỢC nhưng không NẠP

  N NGƯỜI = chunks MÌNH + ACCESS chunks CẢ NHÓM:
    → 100 người × 1000 chunks = 100,000 chunks ACCESSIBLE
    → = Civilization = mạng lưới virtual chunks

  XÃ HỘI HIỆN ĐẠI = virtual chunks ULTIMATE:
    → Không biết phẫu thuật → bác sĩ biết → tôi VẪN ĐƯỢC phẫu thuật
    → Không biết xây nhà → thợ biết → tôi VẪN CÓ nhà
    → 1 người hiện đại ACCESS triệu chunks qua connection
    → Đảo hoang: MẤT TẤT CẢ virtual chunks → quay về stone age


  ⚠️ VIRTUAL CHUNKS CẦN TRUST:

    Chunks CỦA MÌNH: dùng NGAY, tin tuyệt đối (compiled trong body)
    Virtual chunks: cần TRUST người đó

    → Trust = "BANDWIDTH" cho virtual chunks:
      Connection deep + trust cao → virtual chunks RELIABLE
      Connection shallow + trust thấp → virtual chunks UNRELIABLE

    → 3 bạn thân = access SÂU nhưng hẹp (compiled, reliable)
    → 100 người quen = access NHẸ nhưng rộng (surface info, risky)

    → "Cô đơn giữa đám đông" = 300 connections surface + 0 deep
    → Body VẪN lonely vì: deep virtual chunks = 0

    🟡 Consistent với:
      Extended Mind Thesis (Clark & Chalmers 1998):
        knowledge extends beyond individual brain
      Distributed Cognition (Hutchins 1995):
        cognition distributed across people + artifacts
```

---

### §2.4 — Melody Calibration — 2 Bodies Tune Nhau

```
🟡 CONNECTION KHÔNG CHỈ EXTEND — còn CALIBRATE:

  CƠ CHẾ:
    → 2 người ở gần → body CẢ HAI fire input từ NHAU liên tục:
      A nhìn B → visual → adjust → B nhìn A → adjust
      A nghe B → auditory → adjust → B nghe A → adjust
      A relax → B thấy → B relax → A thấy B relax → A relax hơn
    → = Feedback loop LIÊN TỤC giữa 2 bodies
    → = 2 instruments TỰ tune vào nhau — không cần conductor
    → Chủ yếu VÔ THỨC — mirror mechanism chạy tự động
      (chi tiết mirror: Domain-Mechanisms.md §1)


  4 CHỨC NĂNG CALIBRATION:

    ① Error correction — sửa lệch:
       → Bạn stress → body lệch → bạn thân thấy → "ê, sao vậy?"
       → Body được MIRROR bởi body KHÁC → thấy MÌNH qua gương
       → = Observer advantage: người khác thấy pattern MÌNH không thấy
       → PFC mình yếu (mệt) → người kia CALIBRATE HỘ qua input vô thức

    ② Convergence — giống nhau dần:
       → Mới gặp: 2 melody KHÁC → surface adjust
       → Thân vài tháng: overlap TĂNG → nói cùng kiểu, cười cùng chỗ
       → Thân nhiều năm: melody converge SÂU → "hiểu không cần nói"
       → Vợ chồng decades: melody GẦN NHƯ 1 → giống nhau kỳ lạ
       → 🟢 Emotional contagion (Hatfield 1994): emotionally converge
       → 🟢 Interpersonal synchrony (Feldman 2007):
         heart rate, breathing, brain waves ĐỒNG BỘ khi interact

    ③ Dual real-check — 2 body check thay vì 1:
       → 1 người imagine → chỉ MÌNH check → có thể sai
       → 2 người imagine → 2 body check ở 2 vị trí → xác suất đúng TĂNG
       → "Ê, mày nghĩ sao?" = dùng body BẠN check real cho imagine CỦA MÌNH
       → = 2 sensors > 1 sensor → domain map accuracy TĂNG

    ④ Complementary — bù khuyết:
       → A giỏi logic + yếu social → B giỏi social + yếu logic
       → Cùng nhau → melody CHUNG mạnh hơn cả 2
       → = "Ăn ý" = 2 melody BỔ SUNG thay vì trùng


  ƯU ĐIỂM vs NHƯỢC ĐIỂM:

    ƯU ĐIỂM:
      → Sửa lỗi (error correction)
      → Đồng bộ (convergence → hiểu nhau nhanh)
      → Chính xác hơn (dual check)
      → Mạnh hơn (complementary)

    NHƯỢC ĐIỂM:
      → 2 melody khác QUÁ → dissonance mãn tính
        (vợ chồng xung đột dai dẳng)
      → 2 melody CÙNG SAI → reinforce sai → echo chamber
      → Phụ thuộc → 1 mất → melody bị phá nặng
        (grief = melody THIẾU 1 phần đã compiled)
      → Converge QUÁ → mất individuality
        ("sống cuộc đời người kia")


  CO-REGULATION — ứng dụng trực tiếp calibration:

    🟡 Co-regulation = 1 agent CALM calibrate 1 agent STRESSED:

    → 1 người stress + 1 người calm → calm mirror vào stressed
    → Stressed body nhận mirror calm → SHIFT hướng calm dần
    → = "Ôm ai đó calm → MÌNH calm" = mirror mechanism
    → 🟢 Social buffering (Kiyokawa 2004): presence giảm stress response

    PRESENCE = channel RIÊNG:
    → Ngồi cạnh nhau không nói gì → VẪN CALIBRATE
    → Body detect "có agent gần" qua: nhiệt, breathing, micro-movement
    → = Channel mà KHÔNG medium nào replicate được
    → = Tại sao "gặp nhau" KHÁC "video call" dù cùng thấy + nghe
```

---

### §2.5 — Capacity Limits — Tại Sao Không Thể 100 Bạn Thân

```
🟢 CONNECTION CÓ GIỚI HẠN — Dunbar's Number (Dunbar 1992):

  Neocortex size TƯƠNG QUAN group size across primates.
  Người: ~150 social connections tối đa có ý nghĩa.

  ┌──────────┬──────────┬────────────────────────────────────────────┐
  │ Tầng     │ Số người │ Connection depth                            │
  ├──────────┼──────────┼────────────────────────────────────────────┤
  │ Intimate │ ~5       │ Deep trust, daily calibration               │
  │          │          │ Virtual chunks RELIABLE, co-regulate        │
  │          │          │ "Mất 1 người = melody BỊ PHÁ nặng"         │
  ├──────────┼──────────┼────────────────────────────────────────────┤
  │ Close    │ ~15      │ Good trust, weekly calibration              │
  │          │          │ Virtual chunks OK, share feelings           │
  │          │          │ "Mất = buồn, nhưng melody sống"            │
  ├──────────┼──────────┼────────────────────────────────────────────┤
  │ Friends  │ ~50      │ Moderate trust, monthly contact             │
  │          │          │ Virtual chunks surface, share news          │
  │          │          │ "Mất = tiếc, nhưng ít ảnh hưởng"           │
  ├──────────┼──────────┼────────────────────────────────────────────┤
  │ Acquaint.│ ~150     │ Low trust, rare contact, xã giao            │
  │          │          │ Virtual chunks minimal                      │
  │          │          │ "Mất = gần như không biết"                  │
  └──────────┴──────────┴────────────────────────────────────────────┘


  TẠI SAO CÓ GIỚI HẠN:

    ① Time × Attention = BOTTLENECK:
       → 1 bạn thân: gặp hàng tuần × nhiều giờ × nhiều năm = chunks SÂU
       → 100 "bạn thân": 100 × vài giờ/tuần = KHÔNG ĐỦ GIỜ
       → = THỜI GIAN là bottleneck, không phải "tình cảm"

    ② PFC bandwidth CÓ HẠN:
       → Model 5 người SÂU = tốn nhiều chunks + update thường xuyên
       → Model 100 người = chỉ surface (không đủ brain resource)
       → = Giống PFC ~4 dimensions: CEILING hardware

    ③ Trust build CHẬM, mất NHANH:
       → Deep trust: months → years để build
       → Mất trust: 1 lần betray = PHÁ
       → = 5 deep trust = ĐÃ là investment lớn
       → = 100 deep trust = KHÔNG KHẢ THI

  → Quality >>> Quantity:
    3 bạn thân thật = MẠNH HƠN 300 followers
    3 bạn thân: virtual chunks deep + calibration real + co-regulation
    300 followers: no virtual chunks + no calibration + no co-regulation

  ⚠️ SOCIAL MEDIA TRAP:
    → Tạo CẢM GIÁC connection (likes, comments, followers)
    → NHƯNG: không có body-level calibration (touch, presence, co-regulate)
    → Body vẫn LONELY (vì body CẦN real calibration, không phải text)
    → = "Connected online, lonely offline"
```

---

### §2.6 — Distance Spectrum — Khoảng Cách × Calibration Quality

```
🔴 CONNECTION QUALITY TỈ LỆ THUẬN với số channels hoạt động:

  ┌──────────────────┬──────────┬──────────────────────────────────────┐
  │ Medium           │ Fidelity │ Channels hoạt động                   │
  ├──────────────────┼──────────┼──────────────────────────────────────┤
  │ Gặp trực tiếp    │ ~100%    │ Visual+Auditory+Touch+Smell+Presence │
  │                  │          │ +Timing → full body sync              │
  ├──────────────────┼──────────┼──────────────────────────────────────┤
  │ Video call       │ ~60-70%  │ Visual+Auditory+Timing               │
  │                  │          │ MẤT: touch, smell, presence           │
  ├──────────────────┼──────────┼──────────────────────────────────────┤
  │ Voice call       │ ~40-50%  │ Auditory+Timing                      │
  │                  │          │ Giọng = rich (tone, pace, breath)     │
  ├──────────────────┼──────────┼──────────────────────────────────────┤
  │ Text/Chat        │ ~20-30%  │ Visual (chữ) only                    │
  │                  │          │ PFC phải imagine tone → DỄ HIỂU SAI  │
  ├──────────────────┼──────────┼──────────────────────────────────────┤
  │ Ảnh tĩnh         │ ~10-20%  │ 1 frame visual                       │
  │                  │          │ Trigger chunks đã có, không tạo mới  │
  ├──────────────────┼──────────┼──────────────────────────────────────┤
  │ Nhớ trong đầu    │ ~5-15%   │ Thuần imagine → tùy chunk depth      │
  │                  │          │ Bạn thân: rõ. Người quen: mờ.        │
  └──────────────────┴──────────┴──────────────────────────────────────┘


  ⭐ CHUNKS ĐÃ CÓ BÙ cho channels THIẾU:

    Bạn thân + text = VẪN hiểu sâu
      → Chunks deep → imagine ĐÚNG tone
    Người lạ + video call = VẪN hiểu NÔNG
      → Chunks thiếu → miss context

    Quy tắc:
      Chunk depth > channel count ở connection ĐÃ CÓ
      Channel count > chunk depth ở connection MỚI BUILD

    → GẶP TRỰC TIẾP quan trọng nhất ĐỂ BUILD connection mới
    → SAU KHI build: medium ít channels VẪN OK (chunks đã compiled)
```

---

### §2.7 — Connection Biểu Hiện Khác Tùy Context

```
🟡 CONNECTION = 1 PATTERN, NHIỀU BIỂU HIỆN:

  CÙNG 1 connection pattern → KHÁC context → KHÁC biểu hiện:

    Context "agent bị phá":
      → Mirror dissonance → drive GIÚP
      → "Thấy bạn khóc → body mình khó chịu → muốn an ủi"

    Context "MÌNH phá harmony":
      → Social dissonance → drive SỬA
      → "Nói lỡ lời → mặt bạn tối → xấu hổ → muốn xin lỗi"

    Context "melody đồng bộ tốt":
      → Body reward → drive GIỮ + LẶP LẠI
      → "Cả nhóm cười → body sướng → muốn gặp lại"

    Context "MÌNH được HIỂU":
      → Body reward CỰC MẠNH → feel safe
      → "Kể chuyện buồn → bạn gật 'ừ, hiểu' → nhẹ người"

    Context "MẤT connection":
      → Body dissonance → grief / lonely
      → "Bạn thân chuyển đi → body trống → nhớ"

    Context "bị REJECT khỏi nhóm":
      → Body dissonance CỰC MẠNH
      → "Cả lớp tẩy chay → body đau NHƯ đau vật lý"
      → 🟢 Social pain = physical pain pathway (Eisenberger 2003)

    Context "CÙNG vượt khó":
      → Body bond CỰC SÂU → "chiến hữu"
      → "Cùng deadline → xong → bond MẠNH hơn đi chơi 10 lần"

    Context "có agent BÊN CẠNH" (presence only):
      → Body regulate tốt hơn → cortisol GIẢM
      → "Ngồi cạnh bạn im lặng → VẪN tốt hơn ngồi 1 mình"
      → 🟢 Social buffering (Kiyokawa 2004)

  → KHÔNG CẦN liệt kê "bao nhiêu LOẠI connection"
  → CHỈ CẦN: 1 connection pattern + nhiều contexts
  → Mỗi context KÍCH HOẠT pattern theo CÁCH RIÊNG → biểu hiện KHÁC
```

---

## §3 — ENEMY / THREAT PATTERN

```
🟡 ENEMY = MẶT ĐỐI DIỆN CỦA CONNECTION:

  Connection: sustained mutual POSITIVE valence → bond
  Enemy:      sustained NEGATIVE valence → avoid / fight / destroy

  NHƯNG enemy pattern CÓ ĐẶC ĐIỂM RIÊNG — KHÔNG phải "connection ngược":


  DEFINE:

    Enemy Pattern = compiled negative valence relationship
    giữa body và domain entity (agent HOẶC object),
    khi entity LIÊN TỤC ảnh hưởng NEGATIVE tới body channels.

    → Agent enemy: người đe dọa, đối thủ, kẻ hại
    → Object enemy: chất độc, bẫy, vũ khí (hướng về phía mình)
    → Object-as-proxy: bụi cây chiến trường, gói bưu phẩm lạ
      (object + schema "agent nguy hiểm có thể ẩn bên trong")


  ⭐ BẤT ĐỐI XỨNG QUAN TRỌNG — Enemy hình thành NHANH hơn Connection:

    🟢 Negativity bias (Baumeister 2001, Rozin & Royzman 2001):
      → Negative events ảnh hưởng MẠNH hơn positive events cùng magnitude
      → 1 lần bị phản bội > 10 lần giúp đỡ (về mặt valence impact)
      → = Evolutionary logic: miss 1 threat = CHẾT. Miss 1 friend = chỉ thiếu

    Connection: cần NHIỀU lần positive → dần dần emerge
    Enemy: có thể chỉ 1 lần negative MẠNH → NGAY LẬP TỨC compile

    Connection: mất NHANH khi 1 lần violation lớn
    Enemy: mất CHẬM — cần NHIỀU positive để override (nếu có thể)

    → = Asymmetric cost: false negative enemy = chết. False positive = chỉ tốn energy.
    → = Não THIẾT KẾ để enemy pattern NHẠY hơn connection pattern.


  CÁC DẠNG ENEMY PATTERN:

    ① AGENT ENEMY — sustained negative valence từ agent:
       → Kẻ bắt nạt: L0 threat + L2 humiliation + L3 status damage
       → Sếp toxic: L1 power over livelihood + L2 stress + autonomy--
       → Kẻ thù chiến trường: L0 THREAT CỰC CAO → kill or be killed

       Đặc biệt agent enemy:
         → Agent CÓ mục tiêu riêng → unpredictable → khó tránh
         → Agent BIẾT mình đang tránh → adjust strategy → arms race
         → = Agent enemy NGUY HIỂM HƠN object enemy vì adaptive

    ② OBJECT ENEMY — sustained negative valence từ object:
       → Chất độc, lửa, độ cao: L0 threat compiled từ experience/schema
       → Vũ khí hướng về mình: dao trong tay kẻ thù, bom
       → "Gián" / "chuột": L2 disgust compiled (schema inheritance từ culture)

       Object enemy = DỄ TRÁNH hơn agent:
         → Object = deterministic, không adaptive
         → Biết quy tắc (lửa = nóng) → tránh = xong
         → NHƯNG: schema CÓ THỂ khuếch đại (phobia = object enemy quá mức)

    ③ OBJECT-AS-PROXY — object được reclassify vì agent schema:
       → Bụi cây chiến trường: object → "có thể có agent ẩn" → L0 THREAT
       → Gói bưu phẩm lạ: object → "có thể là bom" → L0 THREAT
       → Bẫy gai, mìn: object MANG Ý ĐỒ agent → valence từ agent, dạng object
       → = Schema reclassify: Object-Agent.md §4 (schema override)

       Case bụi cây Việt Cộng (Domain-Interaction.md §2):
         → Bình thường: bụi cây = object, Logic mode, valence neutral
         → Chiến trường: schema "VC ẩn trong bụi" active →
            RECLASSIFY → potential agent container →
            SWITCH Modeling mode → L0 THREAT cực cao
         → = CÙNG entity, KHÁC schema → KHÁC hoàn toàn pattern


  ⭐ DEHUMANIZATION — reclassify agent → object để TIÊU DIỆT:

    🟡 Vấn đề: agent enemy → mirror VẪN fire (vì classified = agent)
      → Mirror fire → dissonance khi gây hại cho họ
      → = RÀO CẢN tự nhiên chống bạo lực (mirror mechanism)

    Giải pháp (từ góc nhìn evolution): RECLASSIFY agent → object
      → Object = không có state → mirror KHÔNG fire
      → = Có thể tiêu diệt MÀ KHÔNG bị mirror dissonance

    Cases:
      → "Gooks", "cockroaches" (Rwanda) = ngôn ngữ dehumanize
        → Relabel agent → vermin/object → bypass mirror
      → "Beef" (Object-Agent.md §4): con bò → thịt
        → Agent → object → eat without mirror guilt
      → Bác sĩ phẫu thuật: patient → "ca mổ" → Object-Logic mode
        → CHỨC NĂNG: giảm mirror → tập trung kỹ thuật (healthy context)

    → Dehumanization = Object-Agent OVERRIDE NGƯỢC (agent→object)
    → Mục đích: tắt mirror → cho phép hành động mà mirror sẽ block
    → = Cùng mechanism, KHÁC moral context:
      Bác sĩ: override TẠM THỜI, mục đích GIÚP = healthy
      Chiến tranh: override TOÀN DIỆN, mục đích DIỆT = dangerous


  ENEMY PATTERN × SCHEMA INHERITANCE:

    🟡 Enemy pattern CÓ THỂ TRUYỀN qua schema KHÔNG cần trải nghiệm:
      → Cha mẹ dạy: "rắn = nguy hiểm" → con compile enemy pattern MÀ CHƯA THẤY rắn
      → Văn hóa dạy: "nhóm X = kẻ thù" → compile TRƯỚC KHI gặp ai thuộc nhóm X
      → = Valence.md §4: schema inheritance = nhanh nhất nhưng ÍT chính xác nhất

    Risk: enemy pattern inherited → OVERGENERALIZE:
      → "1 người nhóm X hại tôi" → compile "TOÀN BỘ nhóm X = enemy"
      → = Cùng mechanism §9 Violation (overgeneralize sau violation)
      → = Prejudice, racism = enemy pattern inherited + overgeneralized
```

---

## §4 — NURTURING PATTERN

> Absorb core concepts từ Empathy-Mirror.md §6.
> Reframe qua Domain Interaction lens.

```
🟡 NURTURING = "CHĂM SÓC" — KHÔNG phải channel mới, là EMERGENT PATTERN:

  DEFINE:
    Nurturing pattern = drive chăm sóc entity có vulnerability cao,
    emerge từ: mirror × vulnerability cues × perceived ability × existing channels.

    = Kết quả tự nhiên khi body:
      ① Classify entity = agent (hoặc living being)
      ② Mirror detect: agent đang có DEFICIT
      ③ Vulnerability cues AMPLIFY mirror signal
      ④ Perceived ability: "tôi CÓ THỂ giúp"
      → Drive chăm sóc FIRE


  6-STEP PATTERN:

    ① DETECT: thấy sinh vật + vulnerability cues
       → Mắt to, body nhỏ, đầu to, biểu cảm yếu ớt
       → 🟢 Baby Schema (Lorenz 1943): hard-wired trigger cho nurturing

    ② MIRROR: body tạo bản sao deficit CỦA HỌ → dissonance CỦA MÌNH
       → Thấy em bé khóc → body MÌNH dissonance (dù mình không đói/đau)
       → Mirror mechanism: Domain-Mechanisms.md §1

    ③ PERCEIVED ABILITY: PFC evaluate "tôi có thể giúp không?"
       → Ability CAO → drive MẠNH ("tôi có đồ ăn, tôi bế được")
       → Ability THẤP → drive BỊ BLOCK → có thể CẮT INPUT (defense)
       → = "Van điều khiển" giữa mirror dissonance và action

    ④ IMAGINE-FINAL: preview "nếu giúp → họ improve → mirror dissonance GIẢM"
       → Body preview kết quả → drive hoặc không

    ⑤ DRIVE → ACTION: chăm sóc (cho ăn, ôm, bảo vệ, dạy)

    ⑥ REWARD: thấy target IMPROVE → mirror REWARD fire → body MÌNH sướng
       → "Nhìn con ăn ngon mà sướng" = mirror reward khi con's L1 met


  ⭐ REWARD ĐẶC BIỆT — gắn vào state NGƯỜI KHÁC:

    Mọi pattern khác: reward từ body-state CỦA MÌNH cải thiện
    Nurturing:         reward từ body-state CỦA NGƯỜI KHÁC cải thiện

    = "Sướng khi THẤY người khác khỏe" = mirror reward
    = Unique — không pattern nào khác hoạt động kiểu này
    = Tại sao nurturing CÓ THỂ override body-base CỦA MÌNH (§5, §8.5 Empathy-Mirror.md)


  4 YẾU TỐ QUYẾT ĐỊNH MIRROR STRENGTH (→ nurturing intensity):

    ① LIVING — phải có sự sống (hoặc perceived):
       → Living → mirror CÓ
       → Non-living → mirror KHÔNG (trừ khi nhân cách hóa)

    ② VULNERABILITY — càng yếu ớt → mirror CÀNG MẠNH:
       → 🟢 Baby Schema (Lorenz 1943, Glocker 2009):
         Mắt to, mặt tròn → nucleus accumbens fire kể cả ở non-parents
       → Evolutionary: vulnerability = potential death → brain auto-prioritize

    ③ EXPRESSIVENESS — càng biểu hiện rõ state → mirror CÀNG MẠNH:
       → Mặt người: CỰC expressive → mirror MẠNH
       → Mặt chó: khá expressive → mirror tốt
       → Cá: gần zero biểu cảm → mirror rất yếu
       → 🟢 Facial EMG mimicry (Dimberg 2000): automatic, 300ms

    ④ SIMILARITY / FAMILIARITY — càng giống/quen → mirror CÀNG MẠNH:
       → Cùng loài > khác loài
       → Quen > lạ (chunks compiled → simulate chính xác)
       → "Ngang tuổi con mình" → map vào nurturing circuit → AMPLIFY
       → 🟢 In-group empathy bias (Xu 2009): ACC mạnh hơn cho in-group

    CÁC YẾU TỐ NHÂN với nhau (KHÔNG cộng):
      Baby ruột = living ✅ × vulnerability MAX × expressiveness CAO × familiarity MAX
      → = Nurturing MẠNH NHẤT

      Em bé lạ = living ✅ × vulnerability CAO × expressiveness CAO × familiarity THẤP
      → = VẪN mạnh (3/4 cao) → giải thích: người lớn thấy em bé lạ → cười

      Cá cảnh = living ✅ × vulnerability THẤP × expressiveness ≈ 0 × familiarity THẤP
      → = Mirror gần ZERO → chủ yếu Aesthetic, ít nurturing


  CASES KIỂM CHỨNG (reframe từ Empathy-Mirror.md §6):

  ┌────────────────────────┬──────────────────────────────────────────────────┐
  │ Case                   │ Domain Interaction analysis                      │
  ├────────────────────────┼──────────────────────────────────────────────────┤
  │ Mẹ chăm con ruột      │ 4 yếu tố MAX + L0 Protect Gene                  │
  │                        │ Mirror reward: "nhìn con ăn ngon mà sướng"      │
  ├────────────────────────┼──────────────────────────────────────────────────┤
  │ Người lớn thấy baby   │ Gene ❌, connection ❌ → VẪN mirror fire         │
  │ LẠ → muốn bế          │ = Baby schema → hardware trigger, KHÔNG cần bond│
  │                        │ = BẰNG CHỨNG nurturing ≠ connection             │
  ├────────────────────────┼──────────────────────────────────────────────────┤
  │ Mẹ VN chăm bộ đội     │ Schema map: "ngang tuổi con" → amplify          │
  │ ngang tuổi con         │ Gầy (L1 deficit) + xa nhà → mirror fire         │
  │                        │ "Coi nó như con" = map vào nurturing circuit     │
  ├────────────────────────┼──────────────────────────────────────────────────┤
  │ Nuôi pet sâu sắc      │ Vulnerability CAO + ability CỰC CAO (100% phụ   │
  │                        │ thuộc) → mirror + mastery stack                  │
  │                        │ "Để chúng nó không bị stress" = model nội tâm   │
  ├────────────────────────┼──────────────────────────────────────────────────┤
  │ Giáo viên thấy trò    │ DOUBLE reward: mirror (trò hiểu) + mastery      │
  │ "AHA!"                 │ (tôi dạy tốt) → stack                           │
  │                        │ "Dạy mà trò hiểu là sướng nhất"                │
  ├────────────────────────┼──────────────────────────────────────────────────┤
  │ Con vật bị thương      │ Ability CAO → dừng giúp → nhẹ nhõm             │
  │ trên đường             │ Ability THẤP → quay mặt → CẮT INPUT (defense)  │
  │                        │ = Perceived ability modulation RÕ NHẤT          │
  ├────────────────────────┼──────────────────────────────────────────────────┤
  │ Chăm cha mẹ già       │ Vulnerability TĂNG + familiarity SÂU             │
  │ (role reversal)        │ + Schema conflict (cha mẹ = người chăm, không   │
  │                        │ phải người ĐƯỢC chăm) → emotional complex       │
  │                        │ Có thể TRÁNH NÉ: mirror quá mạnh + ability     │
  │                        │ thấp ("không thể reverse aging")                │
  ├────────────────────────┼──────────────────────────────────────────────────┤
  │ Chăm cây cảnh         │ Living ✅ nhưng expressiveness ≈ 0               │
  │                        │ → Mirror GẦN KHÔNG → chủ yếu Mastery/Aesthetic  │
  │                        │ "Thấy cây ra hoa mừng" ≠ "thương cây"          │
  └────────────────────────┴──────────────────────────────────────────────────┘

  → MỌI cases consistent với: mirror × vulnerability × ability × channels
  → Nurturing = EMERGENT PATTERN, không phải channel hay module riêng
```

---

## §5 — "CHO ĐI" PATTERN

> Absorb core concepts từ Empathy-Mirror.md §6.5 + §8.5.
> Key insight: "cho đi KHÔNG BAO GIỜ vô tư tuyệt đối."

```
🟡 "CHO ĐI" = CƠ CHẾ PHÂN PHỐI TÀI NGUYÊN TỰ NHIÊN:

  DEFINE:
    "Cho đi" pattern = drive chia sẻ resource (vật chất, kiến thức, hành động)
    emerge khi: body-base CỦA MÌNH đủ + mirror detect NGƯỜI KHÁC thiếu
    + mirror reward (từ sharing) > keep reward (từ giữ).

    = Không cần luật. Không cần đạo đức. Không cần PFC quyết định "nên share."
    = Body TỰ drive share khi THỪA — vì sharing TỐI ƯU HƠN giữ.


  CƠ CHẾ CỐT LÕI:

    Item X ở chỗ TÔI, body-base MÌNH đã ĐỦ:
      → Item X thêm cho MÌNH: reward ≈ 0 (đã no, đã biết, đã có)
      → Body-Satisfaction đã fire → drive dùng thêm = GẦN ZERO

    Item X ở chỗ NGƯỜI KHÁC đang THIẾU:
      → Mirror detect: họ THIẾU → mirror dissonance CỦA MÌNH fire
      → Share Item X → họ improve → mirror reward CỦA MÌNH fire

    → CÙNG 1 quả xoài:
      Tôi ăn thêm (đã no): reward ≈ 0
      Cho người đói: họ no (reward CHO HỌ) + mirror reward (reward CHO TÔI)
    → = Item tạo TỔNG REWARD LỚN HƠN khi được phân phối
    → = Body TỰ TÍNH: "share > giữ" (không cần PFC phân tích)


  ⭐ "CHO ĐI" KHÔNG BAO GIỜ VÔ TƯ TUYỆT ĐỐI:

    Mọi hành vi "cho đi" đều có body-base reward cho NGƯỜI CHO.
    KHÔNG có ngoại lệ. 4 nguồn reward:

    ① MIRROR REWARD — thấy người nhận improve → body mình sướng
       → "Nhìn mặt nó mở quà mới sướng" = mirror CẦN visual input thực
       → Cho trực tiếp > cho gián tiếp (mirror cần input THẬT)

    ② STATUS REWARD — "tôi là người cho" → L3 Status positive
       → "Mạnh thường quân" = title → status → body reward
       → Costly Signal Theory: cho nhiều = signal "tôi có dư" = status cao

    ③ CONNECTION REWARD — share → bond TĂNG → future access virtual chunks
       → Cho bạn → bond TĂNG → bạn sẽ cho lại khi mình cần
       → 🟢 Reciprocal altruism (Trivers 1971): cooperation = optimal long-term

    ④ SCHEMA REWARD — "người tốt" identity confirm
       → Schema "tôi là người tốt" + hành vi cho đi = ALIGN → satisfaction
       → Schema "cúng = phước" + cúng dường = IMAGINE reward (sau này)

    → = "Cho đi vô tư" thực ra = 4 reward sources CÙNG fire
    → = Không phải xấu — là THIẾT KẾ TỐI ƯU (evolution)
    → = Nhóm có cơ chế này > nhóm không có → gene SURVIVE


  VÍ DỤ PHÂN PHỐI — CÙNG 1 CƠ CHẾ:

    Vật chất:
      Nhà thừa quả → cho hàng xóm → mirror reward + connection
      Thức ăn dư → chia nhóm → nhóm khỏe → mirror + group survival

    Kiến thức:
      Biết trick → share → người khác "ồ hay!" → mirror + mastery
      = "Tại sao người ta MUỐN dạy?" → body-level answer

    Hành động:
      Thấy người bị nạn → giúp → họ ổn → mirror + status + connection
      Chi phí: vài phút. Reward: mirror + connection + status

    Kỹ năng:
      Biết sửa xe → giúp hàng xóm → họ đi được → mirror + connection
      = Skill DƯ THỪA TỰ PHÂN PHỐI ra cộng đồng


  ⭐ KHI NÀO "CHO ĐI" DỪNG — VIOLATION TESTS:

    🟡 Nếu cho đi thật sự vô tư → sẽ KHÔNG BAO GIỜ dừng.
    Nhưng thực tế: cho đi LUÔN dừng khi 1 trong 3 điều kiện bị vi phạm:

    ① BODY-BASE CỦA MÌNH THIẾU:
       → Cho khi thừa = vui. Cho khi thiếu = ĐAU.
       → Gia đình đói mà vẫn cho = override (§5.5 bên dưới)
       → = "Cho đi" drive GIẢM khi body-base CỦA MÌNH deficit

    ② SCHEMA GỐC BỊ VI PHẠM:
       → Cho tiền từ thiện → phát hiện tổ chức lừa đảo → DỪNG NGAY
       → Cúng chùa → phát hiện sư phạm giới → DỪNG NGAY
       → "Coi nó như con" → nó lợi dụng → DỪNG
       → = Khi schema bị violate → toàn bộ drive "cho đi" COLLAPSE
       → = KHÔNG phải vô tư: schema drive bị phá → drive MẤT

    ③ RECIPROCITY = 0 kéo dài:
       → Cho 1 lần, 2 lần, 3 lần → không nhận lại gì →
         body dần compile: "entity này = give nhưng KHÔNG reciprocate"
       → Mirror reward VẪN CÓ nhưng: connection reward GIẢM (no reciprocal)
       → Dần dần: total reward < total cost → drive GIẢM → DỪNG
       → 🟢 Reciprocal altruism (Trivers 1971): non-reciprocator bị loại

    → 3 TESTS này CHỨNG MINH: "cho đi" = driven by body-base reward system
    → Nếu thật sự "vô tư" → 3 tests sẽ KHÔNG ảnh hưởng
    → Nhưng thực tế: MỌI case đều dừng khi 1 trong 3 bị vi phạm


  SPECTRUM — từ LÀNH MẠNH tới PATHOLOGICAL:

  ┌─────────────┬──────────────────────────────────────────────────────────┐
  │ Mức         │ Biểu hiện                                                │
  ├─────────────┼──────────────────────────────────────────────────────────┤
  │ LÀNH MẠNH   │ Body-base ĐỦ + share DƯ → mirror + connection + status  │
  │ (cost ≈ 0)  │ Cho quả thừa, share kiến thức, cho tiền lẻ              │
  │             │ → Body không bị ảnh hưởng → PURE positive                │
  ├─────────────┼──────────────────────────────────────────────────────────┤
  │ TRUNG GIAN  │ Body-base OK + bớt chút → vẫn manageable                │
  │ (có cost)   │ Bớt thời gian riêng → dạy miễn phí                     │
  │             │ Chi phần thu nhập → từ thiện                             │
  ├─────────────┼──────────────────────────────────────────────────────────┤
  │ OVERRIDE    │ Body-base BỊ VI PHẠM — 2 loại:                          │
  │             │                                                          │
  │             │ LOẠI A — L0 JUSTIFIED (có ceiling tự nhiên):             │
  │             │   Mẹ ăn ít → con ăn nhiều → mirror + L0 Protect Gene    │
  │             │   = L0 > L1 (priority ĐÚNG THIẾT KẾ)                    │
  │             │   Ceiling: mẹ chết → con chết → gene MẤT                │
  │             │   = Designed override, tạm thời, có giới hạn            │
  │             │                                                          │
  │             │ LOẠI B — SCHEMA HIJACK (KHÔNG CÓ ceiling):              │
  │             │   Cúng dường tới khánh kiệt → "được phước"              │
  │             │   KHÔNG có gene carrier bị đe dọa                       │
  │             │   Schema "phước" KHÔNG verify được → KHÔNG có ceiling   │
  │             │   = Bug: mechanism tốt cho nhóm, hại cá nhân            │
  ├─────────────┼──────────────────────────────────────────────────────────┤
  │ COMPULSIVE  │ Mirror reward compiled SÂU → body CẦN cho đi            │
  │ (nghiện)    │ Nghiện từ thiện: khó khăn VẪN đi                        │
  │             │ = Reward pathway hijack priority                         │
  │             │ = Cùng cơ chế Core §2.5: schema override body-base      │
  └─────────────┴──────────────────────────────────────────────────────────┘

    Ranh giới RÕ:
      Body đủ + share dư = lành mạnh
      Body thiếu + share vì L0 (gene) = justified, có ceiling
      Body thiếu + share vì SCHEMA = hijack, KHÔNG ceiling, CÓ HẠI


  ⭐ POSITIVE-SUM LOGIC — tại sao evolution GIỮ mechanism này:

    🟢 Nhóm CÓ "cho đi" mechanism:
      → Items dư tự phân phối → ÍT lãng phí
      → Kiến thức tự lan truyền → nhóm giỏi nhanh hơn
      → Người khỏe giúp người yếu → nhóm sống sót tốt hơn

    Nhóm KHÔNG CÓ:
      → Items dư nằm 1 chỗ → hỏng/lãng phí
      → Kiến thức giữ riêng → nhóm chậm learn
      → Người yếu tự chết → nhóm mất thành viên → yếu hơn

    → Gene "share khi thừa" THẮNG gene "giữ hết cho mình"
    → 🟢 Reciprocal altruism (Trivers 1971)
    → 🟢 Kin selection (Hamilton 1964): giúp gene-carrier = giúp gene mình
    → 🟡 Group selection (Wilson 2007): nhóm cooperate > nhóm selfish (debated)


  KẾT NỐI VỚI CONFLICT-DYNAMICS:

    🟡 Conflict-Dynamics: Scarcity = engine → ép expand domain → tổng pie TĂNG
    "Cho đi" pattern: Mirror = distributor → phân phối resource DƯ → GIẢM scarcity

    = 2 CƠ CHẾ BỔ SUNG:
      Scarcity TĂNG → ép tìm mới (đẩy)
      Resource DƯ → tự phân phối (kéo)
    → CÙNG KẾT QUẢ: tối ưu resource cho nhóm
    → KHÁC ĐƯỜNG: 1 cái ĐẨY (scarcity), 1 cái KÉO (mirror reward)
```

---

## §6 — DEPENDENCY PATTERN

```
🟡 DEPENDENCY = CẮT KHÔNG ĐƯỢC DÙ MUỐN:

  DEFINE:
    Dependency pattern = entity có valence positive CAO +
    replaceability THẤP + compiled SÂU vào schema.

    → Body MUỐN cắt (vì có channels negative)
    → Nhưng KHÔNG CẮT ĐƯỢC (vì channels positive quá nhiều + irreplaceable)
    → = "Mắc kẹt" — không phải vì yếu đuối, mà vì DEPENDENCY THẬT


  2 YẾU TỐ CỐT LÕI:

    ① VALENCE POSITIVE CAO (nhiều channels):
       → Entity cung cấp cho NHIỀU body-base needs
       → Càng nhiều channels positive → càng khó cắt
       → Mẹ: L0 (sống) + L1 (ăn, mặc) + L2 (comfort, ôm) + Connection
       → = Positive TRÀN NGẬP nhiều channels

    ② REPLACEABILITY THẤP (Valence.md §2 meta-dimension):
       → Khó hoặc KHÔNG THỂ tìm entity khác thay thế
       → Mẹ: 1 người duy nhất, irreplaceable
       → Bạn thân: khó thay, nhưng CÓ THỂ (qua nhiều năm, bạn mới)
       → Bạn B trong lớp: DỄ thay (nhiều bạn khác cũng cho bút)


  DEPENDENCY MATRIX — predict hành vi:

  ┌───────────────────┬───────────────────────┬─────────────────────────┐
  │                   │ Replaceability CAO    │ Replaceability THẤP     │
  ├───────────────────┼───────────────────────┼─────────────────────────┤
  │ Positive valence  │ "Bạn tốt" — vui khi  │ "Tri kỷ / Gia đình"    │
  │ CAO               │ có, OK khi mất        │ → DEPENDENCY MẠNH      │
  │                   │ Thay thế: dễ          │ → Mất = melody crash    │
  ├───────────────────┼───────────────────────┼─────────────────────────┤
  │ Positive valence  │ "Đồng nghiệp" — tiện │ "Mắc kẹt" — không vui  │
  │ THẤP / Mixed      │ khi có, quên khi mất │ nhưng không cắt được   ���
  │                   │ Thay thế: tự nhiên    │ → DEPENDENCY TOXIC      │
  └───────────────────┴───────────────────────┴─────────────────────────┘


  CASES:

    Mẹ — DEPENDENCY MẠNH NHẤT:
      → Positive: L0 + L1 + L2 + Connection = TẤT CẢ channels
      → Replaceability: IMPOSSIBLE (1 mẹ duy nhất)
      → Dù mẹ ép học (autonomy--) → VẪN yêu mẹ
      → Vì: channels positive > channels negative + irreplaceable
      → Mất mẹ = crash NHIỀU channels cùng lúc → grief CỰC NẶNG

    Bạn B — DEPENDENCY THẤP:
      → Positive: L3 nhẹ (cho bút, chơi cùng)
      → Replaceability: CAO (nhiều bạn khác cũng OK)
      → Khi B gây negative > positive → CẮT DỄ
      → Vì: positive không nhiều + dễ thay thế

    Nhân viên ↔ Sếp — DEPENDENCY QUA KHÓA:
      → Positive: L1 (lương) — nhưng bị KHÓA (§2.2 external lock)
      → Replaceability: phụ thuộc thị trường việc làm
      → Tìm việc mới (tăng replaceability) → dependency GIẢM → cắt
      → = Dependency CÓ THỂ GIẢM bằng cách TĂNG replaceability

    Pet — DEPENDENCY BẤT ĐỐI XỨNG:
      → Pet phụ thuộc chủ: 100% (L0 + L1 toàn bộ)
      → Chủ phụ thuộc pet: L2 (comfort, mirror reward) — KHÔNG L0/L1
      → = Pet mất → buồn (L2 crash). Chủ mất → pet CHẾT (L0 crash)

    Chất gây nghiện — DEPENDENCY OBJECT:
      → Cùng mechanism: positive valence CAO (hijack reward) + irreplaceable
      → Body compile: "chỉ substance X cho reward này"
      → Replaceability: gần ZERO (không gì khác cho CÙNG reward)
      → = Dependency pattern KHÔNG chỉ cho agent — object CÓ THỂ


  WITHDRAWAL — khi mất entity có dependency:

    🟡 Dependency compiled SÂU = melody THIẾU 1 phần khi mất:

    → Mất bạn thân: body trống + lonely + mất virtual chunks + mất co-regulation
      Chunks đã compiled "có bạn X" → body VẪN expect X → X không có → dissonance
      = Grief = melody THIẾU phần đã compile thành "luôn có"

    → Mất mẹ: MỌI channels đều bị ảnh hưởng
      L0 (ai bảo vệ?) + L1 (ai chăm?) + L2 (ai comfort?) + Connection
      = Grief CỰC NẶNG + kéo dài (vì compiled SÂU + irreplaceable)

    → Chia tay người yêu: L2 crash + identity crisis (nếu schema gắn vào người đó)
      "Không có anh/cô ấy thì tôi là ai?" = schema dependency, không chỉ emotional

    RECOVERY = compile schema MỚI không có entity đó:
      → Chậm — vì phải VIẾT LẠI schema ở nhiều vị trí
      → Dependency càng sâu → recovery càng lâu
      → Có thể KHÔNG hoàn toàn recover (mẹ: schema vĩnh viễn có slot "mẹ")
```

---

## §7 — MIXED VALENCE (YÊU + GHÉT)

```
🟡 MIXED VALENCE = BÌNH THƯỜNG, KHÔNG PHẢI BẤT THƯỜNG:

  DEFINE:
    Mixed Valence = entity có channels POSITIVE và NEGATIVE cùng lúc.
    = Multi-channel reality — KHÔNG mâu thuẫn.

    Mainstream thường frame "yêu ghét cùng lúc" = ambivalence = cần giải quyết.
    Framework: đây là TRẠNG THÁI BÌNH THƯỜNG cho bất kỳ agent phức tạp nào.
    Vì: agent phức tạp ảnh hưởng NHIỀU channels → gần như CHẮC CHẮN có mixed.


  TẠI SAO MIXED LÀ BÌNH THƯỜNG:

    Agent ĐƠN GIẢN → valence có thể pure:
      → Bạn mới gặp: chỉ biết 1-2 channels → "vui" hoặc "khó chịu"
      → Vì: chưa đủ interaction để nhiều channels activate

    Agent PHỨC TẠP → valence GẦN NHƯ CHẮC CHẮN mixed:
      → Mẹ: cho ăn (L1+) + comfort (L2+) + ép học (autonomy-)
      → Bạn thân: vui (L2+) + novelty (L3+) + đôi khi gây stress (L2-)
      → Sếp: lương (L1+) + mentoring (L3+) + áp lực (cortisol+)
      → = Càng nhiều interaction → càng nhiều channels bị ảnh hưởng
      → = MIXED gần như chắc chắn cho bất kỳ ai ở cùng đủ lâu


  CASES:

    Mẹ — MIXED điển hình nhất:
      Positive: L0 (sống), L1 (ăn, mặc), L2 (ôm, comfort), Connection
      Negative: autonomy-- (ép học), pressure (kỳ vọng), privacy--
      → Yêu VÀ ghét = multi-channel reality
      → KHÔNG mâu thuẫn — chỉ là NHIỀU channels, MỖI CÁI 1 valence

    Công việc — MIXED phổ biến:
      Positive: L1 (lương), L3 (mastery, status), social (đồng nghiệp)
      Negative: L2 (stress, mệt), autonomy-- (ít tự do), time--
      → "Yêu việc" VÀ "ghét việc" = different channels nói chuyện

    Đất nước — MIXED ở scale lớn:
      Positive: identity (L3), belonging, pride (status)
      Negative: bất công, corruption, frustration
      → "Yêu nước" VÀ "ghét hệ thống" = not conflicting

    Bản thân — MIXED hướng vào trong:
      Positive: tự hào về achievements, identity
      Negative: tự ghét vì failures, shame
      → Mixed valence KHÔNG CHỈ cho người khác — cho chính mình cũng CÓ


  RESOLUTION — channels nào "THẮNG":

    🟡 Khi channels conflict → body resolve theo:

    ① Priority system (L0 > L1 > L2 > L3):
       → L0 threat + L2 positive → L0 THẮNG → tránh
       → L1 need + L2 negative → L1 THẮNG → chịu đựng
       → = Layer thấp > layer cao khi conflict

    ② Net valence direction:
       → Tổng positive > tổng negative → approach
       → Tổng negative > tổng positive → avoid
       → NHƯNG: dependency (§6) có thể LOCK approach dù net negative

    ③ Trend — hướng đi:
       → Mixed nhưng ĐANG TỐT LÊN → stay, invest
       → Mixed nhưng ĐANG XẤU ĐI → prepare to cut
       → Body track TREND, không chỉ current state

    → = Mixed valence KHÔNG CẦN "giải quyết"
    → = Đôi khi sống chung với mixed = realistic và healthy
    → = Muốn PURE positive = unrealistic cho agent phức tạp
```

---

## §8 — GROUP DYNAMICS

```
🟡 GROUP = MULTI-AGENT PATTERNS — khi >2 agents tương tác:

  DEFINE:
    Group dynamics = emergent patterns từ N agents tương tác
    đồng thời trong shared domain context.

    = KHÔNG phải "N cặp connection" cộng lại
    = CÓ properties mới EMERGE ở level nhóm mà cặp không có


  ⭐ AMPLIFICATION — reward × N agents:

    🟡 Nhóm KHUẾCH ĐẠI reward so với cặp:

    Novelty amplification:
      → 1 người kể chuyện → 1 người nghe → novelty × 1
      → 1 người kể → 5 người phản ứng KHÁC NHAU → novelty × 5
      → Mỗi phản ứng = prediction error MỚI → more novelty → more reward
      → Case: bàn tán cô giáo áo chấm đỏ — 1 signal → 5 phản ứng → vui gấp bội

    Mirror amplification:
      → 1 người cười → mirror → tôi cười
      → 5 người cười → mirror × 5 → tôi cười MẠNH HƠN NHIỀU
      → Case: đá bóng cùng nhóm — ghi bàn → 5 người ăn mừng → body REWARD CỰC MẠNH
      → 🟢 Emotional contagion scales with group size (Hatfield 1994)

    Combined: novelty × mirror × N agents = reward EXPLOSION
      → Tại sao concert, lễ hội, đá bóng CẢM XÚC MẠNH hơn xem 1 mình
      → 50,000 người cùng hát = mirror × 50,000 (dù yếu mỗi cái)


  VIRTUAL CHUNKS POOLING:

    🟡 Nhóm = thư viện virtual chunks CHIA SẺ:

    → Mỗi người giỏi 1 thứ → nhóm giỏi TẤT CẢ:
      A biết code + B biết design + C biết marketing = startup
      Không ai biết hết → nhưng NHÓM biết hết

    → Trust trong nhóm = bandwidth SHARED:
      Nhóm trust cao → virtual chunks flow TỰ DO giữa thành viên
      Nhóm trust thấp → mỗi người giữ riêng → nhóm YẾU

    → Case nhóm bạn: A learning chunks, C strength chunks
      A yếu thể lực + C mạnh → A ACCESS C's strength qua group
      C kém học + A giỏi → C ACCESS A's knowledge qua group
      = Nhóm > tổng cá nhân riêng lẻ


  REPLACEMENT — khác biệt LỚN với Connection cặp:

    🟡 Trong nhóm: individual agent CÓ THỂ được thay thế.

    → 5 người chơi đá bóng → 1 người chuyển đi → tìm người mới → nhóm VẪN CHƠI
    → 3 đồng nghiệp ăn trưa → 1 nghỉ việc → người mới vào → nhóm ADJUST
    → = Agent TRONG nhóm có replaceability CAO HƠN agent trong cặp intimate

    NHƯNG: có threshold:
    → Core member mất → nhóm BỊ ẢNH HƯỞNG nặng
    → Quá nhiều thay đổi cùng lúc → nhóm GIẢI TÁN
    → = Nhóm có resilience NHƯNG không unlimited


  KHI NÀO NHÓM TAN:

    ① Nhu cầu thay đổi:
       → Nhóm bạn cấp 3 → lên đại học → nhu cầu KHÁC → nhóm mờ dần
       → KHÔNG phải "quên" — body-base needs SHIFT → group không đáp ứng

    ② Dissonance cá nhân vượt benefit:
       → 1 thành viên gây conflict nhiều → net negative cho phần còn lại
       → Nhóm expel hoặc cá nhân rời

    ③ Nhóm khác VUI HƠN:
       → Nhóm mới → nhiều novelty hơn → body PREFER nhóm mới
       → Nhóm cũ → novelty cạn → body dần shift attention
       → = Cùng mechanism Valence update: entity nào reward nhiều hơn → prefer

    ④ Context thay đổi:
       → Nhóm chiến hữu: chiến tranh kết thúc → shared Imagine-Final MẤT
       → Nhóm dự án: dự án xong → mục tiêu chung MẤT → nhóm tự phai
       → = Shared Imagine-Final = KEO DÁN nhóm. Mất keo → nhóm rời


  IN-GROUP / OUT-GROUP — phần mở rộng tự nhiên:

    🟡 Brain TỰ ĐỘNG chia: "nhóm MÌNH" vs "nhóm KHÁC":

    → In-group: positive valence compiled → mirror MẠNH HƠN → empathy MẠNH HƠN
    → Out-group: neutral/negative valence → mirror YẾU HƠN → ít empathy
    → 🟢 In-group empathy bias (Xu 2009): ACC response mạnh hơn cho in-group

    → Hệ quả:
      In-group member bị hại → mirror mạnh → drive giúp
      Out-group member bị hại → mirror yếu → có thể ignore
      → = Cùng event, KHÁC group membership → KHÁC phản ứng hoàn toàn

    → Khi combined với §3 Enemy:
      Out-group + negative valence compiled → GROUP ENEMY pattern
      → Dehumanize cả nhóm → violence ở scale lớn (chiến tranh, diệt chủng)
      → = Dangerous: group enemy pattern + dehumanization = mass violence possible
```

---

## §9 — VIOLATION & RECOVERY

```
🟡 KHI PATTERN BỊ PHÁ — violation là gì xảy ra khi domain thực tế
   XUNG ĐỘT MẠNH với schema đã compiled:

  DEFINE:
    Violation = domain event PHẢN lại pattern đã compiled,
    với magnitude đủ lớn để schema PHẢI update.

    → Bạn thân nói xấu sau lưng (connection pattern violated)
    → Sư phạm giới (schema "sư = thánh thiện" violated)
    → Tổ chức từ thiện lừa đảo (schema "từ thiện = tốt" violated)
    → Mẹ bỏ rơi con (schema "mẹ = an toàn" violated)


  IMPACT = INVESTMENT × VIOLATION SEVERITY:

    🟡 Không phải mọi violation đều ảnh hưởng BẰNG NHAU:

    Investment CAO × Violation NẶNG = IMPACT CỰC LỚN:
      → Bạn thân 10 năm phản bội = melody CRASH
      → Vì: schema compiled SÂU + violation TRỰC TIẾP phủ nhận schema

    Investment THẤP × Violation NẶNG = IMPACT VỪA:
      → Người quen 1 tháng phản bội = khó chịu nhưng OK
      → Vì: schema chưa compiled sâu + valence chưa tích lũy nhiều

    Investment CAO × Violation NHẸ = IMPACT NHẸ:
      → Bạn thân quên sinh nhật = annoying nhưng không phá pattern
      → Vì: violation không đủ lớn để phủ nhận schema gốc

    ┌────────────────────┬──────────────────┬─────────────────────┐
    │                    │ Violation NHẸ    │ Violation NẶNG      │
    ├────────────────────┼──────────────────┼─────────────────────┤
    │ Investment THẤP    │ Bỏ qua           │ Cắt, move on        │
    ├────────────────────┼──────────────────┼─────────────────────┤
    │ Investment CAO     │ Dung nạp          │ ⭐ CRISIS           │
    │                    │                  │ (3 responses below)  │
    └────────────────────┴──────────────────┴─────────────────────┘


  ⭐ 3 RESPONSES KHI PATTERN BỊ PHÁ (investment cao × violation nặng):

    ① RECALIBRATE — update schema phù hợp domain thực tế:
       → "Bạn A phản bội → bạn A CỤ THỂ không đáng tin, nhưng BẠN BÈ nói chung vẫn OK"
       → = Adjust schema cho ENTITY CỤ THỂ, giữ nguyên pattern tổng
       → HEALTHY nhưng KHÓ:
         Cần PFC mạnh + cortisol không quá cao + time
         Cần chấp nhận đau + process + rebuild
       → = "Trưởng thành" = khả năng recalibrate sau violation

    ② OVERGENERALIZE — mở rộng violation ra TOÀN BỘ category:
       → "Bạn A phản bội → ĐÀN ÔNG/PHỤ NỮ đều thế → KHÔNG TIN AI NỮA"
       → = Violation từ 1 entity → generalize thành PATTERN CHO CẢ LOẠI
       → COMMON vì evolutionary safe:
         False positive (không tin ai) = chỉ mất cơ hội
         False negative (tin nhầm) = có thể CHẾT
       → = Prejudice, trust issues, "không dám yêu lại" = overgeneralize
       → 🟢 Consistent với negativity bias (Baumeister 2001): negative generalize nhanh hơn positive

    ③ COLLAPSE — schema LEVEL CAO bị phá → toàn bộ worldview lung lay:
       → "Sư phạm giới → CHÙA là lừa → TÔN GIÁO là lừa → không tin gì nữa"
       → "Mẹ bỏ rơi → GIA ĐÌNH không an toàn → KHÔNG AI an toàn → thế giới nguy hiểm"
       → = Violation đánh vào ANCHOR SCHEMA → cascade xuống tất cả
       → NGUY HIỂM NHẤT nhưng HIẾM (cần violation ở schema rất cao):
         Schema bị phá ở level này = foundation của worldview
         Foundation lung lay → MỌI THỨ build trên đó lung lay theo


  SCHEMA LEVEL QUYẾT ĐỊNH IMPACT:

    🟡 Violation ở LEVEL NÀO → ảnh hưởng BAO NHIÊU:

    ┌──────────────────────┬────────────────────────────────────────────────┐
    │ Schema Level         │ Violation impact                               │
    ├──────────────────────┼────────────────────────────────────────────────┤
    │ Agent CỤ THỂ        │ "Bạn A tệ" → chỉ ảnh hưởng relation với A    │
    │ (thấp nhất)         │ Recover: tương đối nhanh, cắt A xong          │
    ├──────────────────────┼────────────────────────────────────────────────┤
    │ Category             │ "Đàn ông đều tệ" → ảnh hưởng MỌI relation    │
    │                      │ với đàn ông. Recover: lâu, cần nhiều positive │
    │                      │ counter-evidence                               │
    ├──────────────────────┼────────────────────────────────────────────────┤
    │ Tổ chức / Domain     │ "Tôn giáo là lừa" → ảnh hưởng toàn bộ       │
    │                      │ spiritual domain + trust institutions          │
    │                      │ Recover: rất lâu, có thể KHÔNG                │
    ├──────────────────────┼────────────────────────────────────────────────┤
    │ Worldview            │ "Không ai đáng tin" / "Thế giới nguy hiểm"   │
    │ (cao nhất)           │ → ảnh hưởng MỌI domain interaction            │
    │                      │ Recover: cực kỳ khó, cần anchor MỚI          │
    └──────────────────────┴────────────────────────────────────────────────┘


  CASES PHÂN TÍCH:

    Tình yêu phản bội:
      Investment: CAO (năm, compiled deep, identity gắn vào)
      Violation: NẶNG (trust phá, intimacy bị betray)
      Response phổ biến: overgeneralize ("không dám yêu lại")
      Response healthy: recalibrate ("người NÀY không ổn, nhưng tình yêu vẫn có thể")
      Schema level: agent cụ thể → nhưng DỄ trượt sang category ("đàn ông/phụ nữ")

    Sư phạm giới:
      Investment: CAO (faith compiled deep, often childhood)
      Violation: NẶNG (anchor schema "sư = thánh thiện" bị phá)
      Response phổ biến: overgeneralize ("chùa nào cũng thế") hoặc COLLAPSE ("mất đức tin")
      Schema level: tổ chức/domain → cascade risk CAO
      Đặc biệt: schema "phước" KHÔNG verify được → violation phá foundation unverifiable

    Từ thiện phốt (tổ chức từ thiện lừa đảo):
      Investment: VỪA (tiền + schema "từ thiện = tốt")
      Violation: VỪA-NẶNG (trust phá + "cho đi" reward bị invalidate)
      Response phổ biến: overgeneralize ("từ thiện đều lừa đảo")
      Response healthy: recalibrate ("tổ chức NÀY lừa, tổ chức khác vẫn OK")

    Công đức chùa + sư phạm giới (STACK):
      Investment: CỰC CAO (tiền + time + faith + identity)
      Violation: CỰC NẶNG (sư mà giới = phá anchor)
      → Stack: "cho đi" pattern BỊ PHÁ + faith schema BỊ PHÁ = DOUBLE violation
      → Response thường: collapse ("mất tất cả, tin vào gì bây giờ?")
      → Recovery: cực khó vì 2 patterns bị phá ĐỒNG THỜI


  RECOVERY PATH:

    🟡 Sau violation, pattern CÓ THỂ rebuild — nhưng KHÁC:

    ① Rebuild CÙNG entity (recalibrate):
       → Bạn phản bội → xin lỗi → dần trust lại
       → Rebuild CHẬM HƠN build lần đầu (trust mới < trust cũ thời gian dài)
       → "Vết nứt đã sửa" — functional nhưng sẹo vẫn có trong schema

    ② Rebuild VỚI entity MỚI (replace):
       → Chia tay → gặp người mới → build connection mới
       → Nhưng: overgeneralize schema CÓ THỂ BLOCK (§9 response ②)
       → = "Không dám yêu" = overgeneralize CHẶN rebuild

    ③ Rebuild SCHEMA level cao (nếu collapse):
       → Cần ANCHOR MỚI — entity hoặc experience đủ mạnh
         để overwrite collapsed schema
       → "1 anchor person đủ" — 1 người đáng tin = có thể rebuild
         "thế giới có người tốt" từ collapsed worldview
       → CỰC CHẬM + cần consistency lâu dài
```

---

## §10 — HONEST ASSESSMENT

```
  ESTABLISHED (🟢) — evidence rõ, framework DỰA TRÊN:

    🟢 Social Baseline Theory (Coan & Beckes 2015):
       brain defaults to social proximity, alone = deviation
    🟢 Mere Presence Effect (2024-2025):
       passive social recalibration, auto brain network modulation
    🟢 Bucharest Early Intervention Project (Nelson et al.):
       agent deprivation = measurable brain damage in children
    🟢 Solitary confinement effects (Grassian 2006):
       cognitive deterioration in DAYS despite L1 met
    🟢 Harlow's monkeys (1958):
       touch/presence = separate need, not substituted by food
    🟢 Dunbar's Number (Dunbar 1992, 1998):
       neocortex size ~ group size, ~5/15/50/150 layers
    🟢 Social Brain Hypothesis (Dunbar 1998):
       neocortex evolved primarily for social complexity
    🟢 Emotional contagion (Hatfield 1994):
       emotions spread automatically between people
    🟢 Interpersonal synchrony (Feldman 2007):
       heart rate, breathing, brain waves sync during interaction
    🟢 Social buffering (Kiyokawa 2004):
       presence of others reduces stress response
    🟢 Social pain = physical pain pathway (Eisenberger 2003):
       rejection activates same neural pathways as physical pain
    🟢 Baby Schema / Kindchenschema (Lorenz 1943, Glocker 2009):
       nucleus accumbens fires for infant faces, even in non-parents
    🟢 Facial EMG mimicry (Dimberg 2000):
       automatic, pre-conscious, 300ms
    🟢 In-group empathy bias (Xu 2009):
       ACC response stronger for in-group members
    🟢 Reciprocal altruism (Trivers 1971):
       cooperation = optimal long-term strategy
    🟢 Kin selection (Hamilton 1964):
       helping gene-carriers = helping own genes survive
    🟢 Attachment theory (Bowlby 1969):
       connection = innate drive, proximity seeking
    🟢 Negativity bias (Baumeister 2001, Rozin & Royzman 2001):
       negative events impact stronger than positive events of same magnitude
    🟢 Social isolation → health damage (Holt-Lunstad 2015):
       loneliness = harmful as smoking 15 cigarettes/day


  FRAMEWORK SUY LUẬN (🟡) — logic consistent, framework-specific framing:

    🟡 "Emergent patterns = compiled valence relationships stored in schema"
       — Logical: repeated interaction + valence accumulation → stable patterns
       — Framework-specific: "emergent pattern" terminology là framework framing
       — Consistent với: attachment theory, relationship science, schema therapy

    🟡 "Connection = emergent pattern từ 4 điều kiện"
       (agent input need + mutual positive valence + repeated interaction + time)
       — Reframe Connection.md: từ "emergent property giữa melodies" →
         "emergent pattern trong Domain Interaction"
       — Components đều có evidence riêng; integration = framework

    🟡 "Connection = force multiplier cho toàn bộ body-base"
       — Consistent với: social support → health outcomes literature,
         cooperative advantage (division of labor), social buffering
       — "Force multiplier" table = framework illustration, không phải measured

    🟡 "Virtual chunks" — access chunks người khác qua connection
       — Consistent với: Extended Mind Thesis (Clark & Chalmers 1998),
         Distributed Cognition (Hutchins 1995)
       — Metaphor chưa có formal model hoặc measurement

    🟡 "Melody calibration — 4 functions"
       (error correction, convergence, dual check, complementary)
       — Consistent với interpersonal synchrony research, co-regulation literature
       — 4-function categorization = framework-specific

    🟡 "Connection × Imagine-Final per layer → bond durability"
       — Logical: deeper shared goals → more durable bonds
       — Consistent với self-determination theory (intrinsic > extrinsic)
       — Layer → durability mapping chưa có direct test

    🟡 "External lock" (bond bị khóa vào layer thấp)
       — Consistent với extrinsic motivation research
       — "Lock" mechanism = framework framing cho observed dependency

    🟡 "Enemy hình thành NHANH hơn connection"
       — Directly supported by negativity bias research (🟢 above)
       — Asymmetry claim = logical extension, well-supported

    🟡 "Dehumanization = agent→object reclassification"
       — Consistent với: dehumanization research (Haslam 2006),
         infrahumanization (Leyens 2000)
       — "Agent→object" framing = Domain Interaction specific,
         underlying phenomenon well-documented

    🟡 "Nurturing = mirror × vulnerability × ability × existing channels"
       — Components đều có evidence riêng (baby schema, mirror neurons,
         bystander effect → ability modulation)
       — Combination model = framework synthesis

    🟡 "'Cho đi' không vô tư — 4 nguồn reward"
       — Consistent với reciprocal altruism, costly signaling theory,
         warm glow effect (Andreoni 1990)
       — "Không bao giờ vô tư" = strong claim, mostly supported
       — Edge case: anonymous giving (status reward reduced) — vẫn có mirror + schema

    🟡 "3 violation tests" (body thiếu / schema phá / reciprocity = 0)
       — Logical: nếu vô tư → tests không ảnh hưởng. Thực tế: có ảnh hưởng
       — Consistent với reciprocal altruism, fairness research
       — "Test" framing = framework-specific verification method

    🟡 "Dependency = positive valence + irreplaceable"
       — Consistent với attachment theory, interpersonal dependency research
       — 2-factor model (valence × replaceability) = framework simplification

    🟡 "Mixed valence = bình thường cho agent phức tạp"
       — Consistent với: ambivalence research, dialectical behavior theory
       — Framework contributes: WHY mixed is normal (multi-channel)
       — Resolution model (priority + net + trend) = framework-specific

    🟡 "Group amplification: novelty × mirror × N"
       — Emotional contagion scales with group (🟢)
       — Exact mechanism (novelty × mirror × N = explosion) = framework framing

    🟡 "Violation impact = investment × severity"
       — Logical: well-established in relationship science
       — 3 responses (recalibrate / overgeneralize / collapse) =
         framework categorization of observed post-violation patterns
       — Consistent với: schema therapy, cognitive distortion research

    🟡 "Schema level → violation impact"
       — Logical: higher-level schema = more dependent schemas below
       — Consistent với: foundational beliefs research (Janoff-Bulman 1992)
       — 4-level hierarchy = framework-specific


  HYPOTHESIS (🔴) — logic hợp lý, cần verify thêm:

    🔴 "Connection KHÔNG phải channel mà là emergent pattern"
       — Strong restructure claim, thay đổi Core architecture
       — Cần verify: có testable prediction nào phân biệt
         "channel" vs "emergent pattern" không?

    🔴 "Virtual chunks" — chunk depth > channel count cho existing connection
       — Logical nhưng chưa có controlled experiment
       — Prediction: bạn thân qua text > người lạ qua video —
         testable nhưng chưa test

    🔴 Distance spectrum % numbers (~100%, ~60-70%, ~40-50%,...)
       — Ước lượng, KHÔNG phải đo trực tiếp
       — Relative ordering có thể đúng, absolute % = rough estimate

    🔴 "1 pattern × N contexts" cho connection
       — Logical reframe nhưng chưa có neuroscience test
       — Nếu 1 pattern: damage 1 → mất TẤT CẢ biểu hiện cùng lúc
       — Nếu nhiều mechanisms: damage 1 → cái khác VẪN CÒN → testable

    🔴 "Object-as-proxy enemy" (bụi cây → agent container)
       — Logical extension of Object-Agent schema override
       — Specific mechanism chưa mapped (how exactly does reclassification trigger?)

    🔴 "Schema inheritance → enemy pattern → prejudice"
       — Each step has evidence: schema inheritance (social learning),
         negative generalization (negativity bias), prejudice (social psych)
       — Full causal chain = framework hypothesis

    🔴 "4 factors NHÂN (không cộng) cho mirror strength"
       — Each factor documented, multiplication model = framework hypothesis
       — Could also be additive or more complex interaction

    🔴 "Dependency matrix predict hành vi"
       — Logical 2×2 but real behavior likely more complex
       — Useful heuristic, not precise prediction tool

    🔴 "Group dissolves for 4 specific reasons"
       — Logical categorization of observed patterns
       — Real group dissolution likely involves multiple simultaneous factors

    🔴 "Collapse → cần anchor MỚI để rebuild worldview"
       — Consistent với therapeutic approaches (finding meaning)
       — "1 anchor person đủ" = hopeful hypothesis, not verified


  ⚠️ TỔNG THỂ:
    File này SYNTHESIZE nhiều concepts từ 2+ sessions phân tích sâu.
    Individual components phần lớn established hoặc well-reasoned.
    INTEGRATION vào "Emergent Patterns" framework = MỚI.
    Cần test: predictions cụ thể từ mỗi pattern có match observations không?
    Mạnh nhất: Connection redefine (agent input evidence 🟢 rất mạnh).
    Yếu nhất: Group dynamics, specific % numbers, multiplication model.
```

---

## §11 — CROSS-REFERENCES

```
  DOMAIN INTERACTION FILES (cùng thư mục):
  → Domain-Interaction.md: overview flow — file NÀY = §7 (patterns) chi tiết
  → Object-Agent.md §4: schema override — basis cho §3 dehumanization, §6 reclassify
  → Valence.md: multi-channel evaluation — basis cho MỌI patterns
  → Valence.md §2: meta-dimensions (trust, replaceability) → §6 Dependency, §9 Violation
  → Valence.md §5: update mechanisms → §9 Violation
  → Domain-Mechanisms.md: mirror mechanism → basis cho §4 Nurturing, §5 "Cho đi"
     (file sẽ được tạo)

  CORE FILES:
  → Logic-Modeling.md: 2 processing modes — referenced from §3 (enemy reclassify → switch mode)
  → Drive/Drive.md: HOW drives combine → action (bước SAU patterns)
  → Drive/Novelty.md: novelty drive → §8 Group amplification
  → Drive/Threat.md: threat drive → §3 Enemy pattern, §9 Violation
  → Core-v7.5-Draft.md §2: Body-Base L0-L3 — channels mà patterns ảnh hưởng
  → Core-v7.5-Draft.md §2.5: schema override spectrum — §5 spectrum parallels

  BACKUP FILES (content absorbed):
  → Connection.md (backup): §1-§7.5 absorbed vào §2 Connection Pattern
     Content preserved: emergent property, force multiplier, virtual chunks,
     melody calibration, Dunbar, distance spectrum, 1 drive × N contexts,
     Connection × Imagine-Final
  → Empathy-Mirror.md (backup): §6-§8.5 absorbed vào §4 Nurturing + §5 "Cho đi"
     Content preserved: nurturing pattern, vulnerability cues, perceived ability,
     resource distribution, empathy fatigue (partially → Domain-Mechanisms.md),
     mirror reward override spectrum

  RELATED FILES:
  → Imagine-Final.md §1: 2-tầng structure → §2.2 Connection × Imagine-Final per layer
  → Imagine-Final.md §6: social scale → §8 Group dynamics
  → Schema/Anchor-Schema.md: anchor concept → §9 Violation collapse
  → Cortisol-Baseline.md: sustained stress → §4 mirror overload (fatigue)
  → Body-Dissonance.md: dissonance signals → §2.7 (contexts create specific dissonance)
  → Conflict-Dynamics.md §7: scarcity engine → §5 counter-mechanism (distribute surplus)
  → Knowledge-Flow.md §1: output A→B→C → §5 knowledge distribution
  → Global-Melody.md §1: melody sync → §2.4 calibration mechanism
  → Personal-Melody.md §12: predict melody người khác → §2.4 calibration
  → Relationships.md §4-5: "hiểu nhau" + convergence → §2.4
  → Empathy-Education.md §3: empathy innate → §4 Nurturing evidence
  → Empathy-Education.md §3.6: "1 anchor person đủ" → §9 Recovery
  → Education-Bridge.md §3: threat overdose → §2.2 external lock (khóa L0)
```

---

> *Emergent Patterns — "Bạn không QUYẾT ĐỊNH yêu ai hay ghét ai.
> Body bạn gặp họ, đánh giá họ, lặp lại nhiều lần,
> và 1 ngày bạn nhận ra: pattern ĐÃ HÌNH THÀNH.
> Connection, kẻ thù, người nuôi dưỡng — tất cả EMERGE,
> từ cùng 1 bộ cơ chế, chạy hàng triệu lần, vô thức.
> Bạn chỉ NHẬN RA khi pattern đã quá mạnh để bỏ qua."*
